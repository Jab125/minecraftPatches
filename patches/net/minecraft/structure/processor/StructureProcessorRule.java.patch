--- a/net/minecraft/structure/processor/StructureProcessorRule.java
+++ b/net/minecraft/structure/processor/StructureProcessorRule.java
@@ -34,7 +34,7 @@
       this.locationPredicate = locationPredicate;
       this.positionPredicate = positionPredicate;
       this.outputState = outputState;
-      this.outputNbt = (NbtCompound)nbt.orElse((Object)null);
+      this.outputNbt = (NbtCompound)nbt.orElse(null);
    }
 
    public boolean test(BlockState input, BlockState currentState, BlockPos originalPos, BlockPos currentPos, BlockPos pivot, Random random) {
