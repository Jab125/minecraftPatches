--- a/net/minecraft/entity/ai/brain/ScheduleBuilder.java	1970-01-12 08:58:04
+++ b/net/minecraft/entity/ai/brain/ScheduleBuilder.java	2023-03-30 20:42:23
@@ -19,7 +19,7 @@
    }
 
    public Schedule build() {
-      ((Set)this.activities.stream().map(ScheduleBuilder.ActivityEntry::getActivity).collect(Collectors.toSet())).forEach(this.schedule::addActivity);
+      (this.activities.stream().map(ScheduleBuilder.ActivityEntry::getActivity).collect(Collectors.toSet())).forEach(this.schedule::addActivity);
       this.activities.forEach((activity) -> {
          Activity activity2 = activity.getActivity();
          this.schedule.getOtherRules(activity2).forEach((rule) -> rule.add(activity.getStartTime(), 0.0F));
