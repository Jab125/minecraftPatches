--- a/net/minecraft/client/texture/atlas/Sprite.java
+++ b/net/minecraft/client/texture/atlas/Sprite.java
@@ -63,7 +63,7 @@
    public void close() {
       int i = this.regionCount.decrementAndGet();
       if (i <= 0) {
-         NativeImage nativeImage = (NativeImage)this.image.getAndSet((Object)null);
+         NativeImage nativeImage = (NativeImage)this.image.getAndSet(null);
          if (nativeImage != null) {
             nativeImage.close();
          }
