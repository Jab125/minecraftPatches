--- a/net/minecraft/world/level/storage/LevelStorage.java	1970-01-12 08:58:04
+++ b/net/minecraft/world/level/storage/LevelStorage.java	2023-03-30 20:05:23
@@ -109,8 +109,8 @@
          }
       }
 
-      Dynamic<T> dynamic2 = DataFixTypes.WORLD_GEN_SETTINGS.update(dataFixer, dynamic, version);
-      return WorldGenSettings.CODEC.parse(dynamic2);
+      Dynamic<T> dynamic3 = DataFixTypes.WORLD_GEN_SETTINGS.update(dataFixer, dynamic, version);
+      return WorldGenSettings.CODEC.parse(dynamic3);
    }
 
    private static DataConfiguration parseDataPackSettings(Dynamic<?> dynamic) {
@@ -301,7 +301,6 @@
    }
 
    public static record LevelList(List<LevelStorage.LevelSave> levels) implements Iterable<LevelStorage.LevelSave> {
-      final List<LevelStorage.LevelSave> levels;
 
       public boolean isEmpty() {
          return this.levels.isEmpty();
@@ -531,4 +530,4 @@
          this.lock.close();
       }
    }
-}
+}
\ No newline at end of file
